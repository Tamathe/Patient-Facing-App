import { AppShell } from "@/components/app-shell";

export default function NumbersPage() {
  return (
    <AppShell title="My Numbers">
      <p>Your latest home measurements will land here, so you can keep an easy eye on what your body is doing.</p>
    </AppShell>
  );
}
