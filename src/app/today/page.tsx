import { AppShell } from "@/components/app-shell";

export default function TodayPage() {
  return (
    <AppShell title="Today">
      <p>Your home dashboard is ready to track what matters today, from your tasks to what feels most important right now.</p>
    </AppShell>
  );
}
